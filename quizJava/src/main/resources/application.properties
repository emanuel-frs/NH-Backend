spring.application.name=quiztion

# Configuração do Banco de Dados Supabase (Session Pooler)
spring.datasource.url=jdbc:postgresql://aws-0-sa-east-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.xwcfruivnsudpaixhozd
spring.datasource.password=Em.23170829@14
spring.datasource.driver-class-name=org.postgresql.Driver

# Dialeto do Hibernate para PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Atualização automática do banco de dados (apenas para desenvolvimento)
spring.jpa.hibernate.ddl-auto=update

# Exibir SQL gerado pelo Hibernate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Desativa o Flyway
spring.flyway.enabled=false